# Vercel's Python runtime installs from this file; pyproject.toml stays the
# source of truth for local work (uv sync). Keep the two in step.
#
# uvicorn is deliberately absent: Vercel supplies the server, and the wheel
# would only add to a bundle that has a 250MB ceiling.
fastapi>=0.141.1
httpx>=0.28.1
pdfplumber>=0.11.10
python-docx>=1.2.0
rarfile>=4.5
tiktoken>=0.13.0
google-genai>=1.5.0
pinecone>=5.0.0
python-dotenv>=1.0.0
# The job table (app.firestoreJobStore).
firebase-admin>=6.4.0
# Dispatching ingestion to workers (app.celeryApp). redis is the broker
# client; swapping in RabbitMQ means amqp here and a different broker URL,
# and no code change.
celery>=5.4
redis>=5.0
